enclave {
    // it needs to be judged to put edl-import at here or tested-sgxapp
    from "sgx_tstdc.edl" import *;
    
    include "stdint.h"
    include "stdbool.h"

    untrusted {
        void sgxsan_ocall_init_shadow_memory(uint64_t enclave_base, uint64_t enclave_size, [out] uint64_t *cntrs_copy_start, [out] uint64_t *cntrs_copy_end, [out] uint64_t *pcs_copy_start, [out] uint64_t *pcs_copy_end);
        void sgxsan_ocall_print_string([in, string] const char *str);
        void sgxsan_ocall_addr2line([in, count=arr_cnt] uint64_t *addr_arr, size_t arr_cnt, int level);
    };

};